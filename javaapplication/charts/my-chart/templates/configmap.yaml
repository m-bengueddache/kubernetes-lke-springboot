{{- if .Values.configMap.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configMap.name }}
data:
  {{- range .Values.configMap.data }}
  {{ .key }}: {{ .value | quote }}
  {{- end }}
{{- end }}


    