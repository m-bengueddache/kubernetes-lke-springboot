{{- if .Values.secret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
type: Opaque
data:
  {{- range .Values.secret.data }}
  {{ .key }}: {{ .value | b64enc }}
  {{- end }}
{{- end }}